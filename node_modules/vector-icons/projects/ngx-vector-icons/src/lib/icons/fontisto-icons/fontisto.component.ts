import { Component, Input, ViewEncapsulation } from '@angular/core';

@Component({
  selector: 'fontisto-icons',
  templateUrl: './fontisto.component.html',
  styleUrls: ['./fontisto.component.css'],
  encapsulation: ViewEncapsulation.None,
  host: {
    class: 'fontisto-icons',
  },
})
export class FontistoComponent {
  @Input() name: string = '';
  @Input() color: string = '';
  @Input() size: number = 20;
}
